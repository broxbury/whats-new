// import React from 'react';
// import NewsContainer from './NewsContainer';
// import { render } from '@testing-library/react';
// import '@testing-library/jest-dom';
// jest.mock('models/__mocks__/local')

// describe('NewsContainer', () => {
//   it('displays correct information in the NewsArticle', () => {
//     const { getByText } = render(<NewsContainer currentTopic={local}/>);

//     expect(getByText("Giant Chicken Skeleton In Denver’s Central Library Stands For More Than Just Dinner")).toBeInTheDocument(); 
//     expect(getByText("Car2Go, the popular car-sharing and rental company, will be leaving Denver at the end of the October, the company announced Friday.")).toBeInTheDocument(); 
//   })
// })
